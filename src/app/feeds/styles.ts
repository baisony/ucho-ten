import { tv } from "@nextui-org/react"

export const layout = tv({
    slots: {
        background: "bg-white dark:bg-[#16191F]",
        FeedCard:
            "flex items-center w-full h-[80px] justify-between select-none cursor-pointer bg-white text-black dark:bg-[#16191F] dark:text-white",
    },
})
