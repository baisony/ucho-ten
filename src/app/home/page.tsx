import { SwiperPage } from "@/app/_components/SwiperPage"

const Root = () => {
    return <SwiperPage page={"home"} />
}

export default Root

export const metadata = {
    robots: "nofollow",
    title: "Home",
}
